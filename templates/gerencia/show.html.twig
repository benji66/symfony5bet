{% extends '@FgimenezAdminlte/layout.admin.html.twig' %}

{% block title %}Gerencia{% endblock %}

{% block body %}
    <h1>Gerencia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gerencium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ gerencium.nombre }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ gerencium.createdBy }}</td>
            </tr>
            <tr>
                <th>UpdatedBy</th>
                <td>{{ gerencium.updatedBy }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ gerencium.createdAt ? gerencium.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ gerencium.updatedAt ? gerencium.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gerencia_index') }}">

        <i class="fas fa-arrow-left"></i>
        back to list

    </a>

    <a href="{{ path('gerencia_edit', {'id': gerencium.id}) }}">
        
         <i class="fas fa-edit"></i>
        edit

    </a>

    {{ include('gerencia/_delete_form.html.twig') }}
{% endblock %}
