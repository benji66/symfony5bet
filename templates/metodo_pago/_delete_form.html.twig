<form method="post" action="{{ path('metodo_pago_delete', {'id': metodo_pago.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metodo_pago.id) }}">
    <button class="btn"><i class="far fa-trash-alt"></i>Borrar</button>
</form>
