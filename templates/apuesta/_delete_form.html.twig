<form method="post" action="{{ path('apuesta_delete', {'id': apuesta.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apuesta.id) }}">
    <button class="btn"><i class="far fa-trash-alt"></i>Borrar</button>
</form>
