{% extends '@FgimenezAdminlte/layout.admin.html.twig' %}

{% block title %}Apuesta{% endblock %}

{% block body %}
    <h1>Apuesta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apuestum.id }}</td>
            </tr>
            <tr>
                <th>Monto</th>
                <td><i class="fas fa-dollar-sign"></i> {{ apuestum.monto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('apuesta_index') }}">

        <i class="fas fa-arrow-left"></i>
        volver

    </a>

    <a href="{{ path('apuesta_edit', {'id': apuestum.id}) }}">
        
         <i class="fas fa-edit"></i>
        edit

    </a>
    {{ include('apuesta/_delete_form.html.twig') }}
{% endblock %}
