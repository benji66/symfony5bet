<form method="post" action="{{ path('perfil_banco_delete', {'id': perfil_banco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perfil_banco.id) }}">
    <button class="btn"><i class="far fa-trash-alt"></i>Borrar</button>
</form>
