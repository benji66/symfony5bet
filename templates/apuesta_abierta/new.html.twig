{% extends '@FgimenezAdminlte/layout.admin.html.twig' %}

{% block title %}New ApuestaPropuesta{% endblock %}

{% block body %}
    <h1>Create new ApuestaPropuesta </h1>
{% for label, messages in app.flashes %}
    {% for message in messages %}
    
        <div class="alert alert-{{ label }} alert-dismissible">
                  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                  <!-- <h5><i class="icon fas fa-check"></i> Alert!</h5> -->
                  {{ message }}
        </div>
    {% endfor %}
{% endfor %}
       <div class="row">
          <!-- left column -->
          <div class="col-md-6">
            <!-- general form elements -->
            <div class="card card-primary">
              <div class="card-header">
                <h3 class="card-title">Nuevo</h3>
              </div>
              <!-- /.card-header -->
              <!-- form start -->            
                <div class="card-body">
                   {{ include('apuesta_abierta/_form.html.twig') }}
                </div>
                <!-- /.card-body -->               
             
            </div>
            <!-- /.card -->

              <a href="{{ path('carrera_abierta_show', {'id': carrera.id}) }}">
            	<i class="fas fa-arrow-left"></i>
            	back to list
            	
            </a>
		  </div>
		</div>
            
{% endblock %}

{% block javascripts %}
 <script> 
    $('#caballos_boton').click(function(){
            var datos_caballos_id =$('#caballos_texto').val(); 
            //alert(datos_caballos_id);          

            if(!$("#datos_caballos_id_"+datos_caballos_id).val() && datos_caballos_id!=''){
                
                $("#datos_caballos").append('<li id="datos_caballos_texto_'+datos_caballos_id+'"> <a onclick="$(\'#datos_caballos_texto_'+datos_caballos_id+'\').remove()" href="#"><i class="far fa-circle nav-icon">X</i></a>  '+ datos_caballos_id +' <input type="hidden" id="datos_caballos_id_'+ datos_caballos_id +'" name="datos_caballos[]" value="'+ datos_caballos_id+'"></li>');               
            }   

           $('#caballos_texto').val('');        

    });

 </script>  



 {% endblock %}    